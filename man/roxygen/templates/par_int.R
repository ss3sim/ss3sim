#' @param par_int A vector of initial values, one for each entry in
#'   \code{par_name}. Values of \code{NA} leave the `INIT` value for
#'   that parameter at the value found in the \code{.ctl} file.
